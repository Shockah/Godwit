apply plugin: "kotlin"

dependencies {
	compile project(":core")
	compile project(":tree-nodes")
}

sourceSets.main {
	kotlin.srcDirs = ["src"]
	resources.srcDirs = ["assets"]
}

compileKotlin {
	kotlinOptions {
		jvmTarget = "1.8"
		freeCompilerArgs = ["-XXLanguage:+InlineClasses"]
	}
}